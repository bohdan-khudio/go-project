CREATE TABLE events (
                          id    integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                          title  varchar NOT NULL,
                          short_desc varchar,
                          description varchar,
                          long numeric,
                          lat numeric

);

CREATE TABLE users (
                          id    integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                          name varchar NOT NULL
);